# 隐私说明

Reading Mirror 会处理高度个人化的 Apple 备忘录和微信读书数据。安装前请理解以下数据边界。

## Skill 发布包不包含什么

本仓库不包含任何用户的：

- `WEREAD_API_KEY` 或其他认证信息；
- Apple 备忘录正文、标题或 Note ID；
- 微信读书书架、划线、书评或个人笔记；
- Reading Mirror 运行状态或本地镜像。

每位用户首次运行时会在自己的设备上创建独立数据目录。

## 运行时会读取什么

- 用户通过 Note ID、准确标题或连续原文明确指定的 Apple 备忘录；
- 为进行连续原文定位而在本机比较的 Apple Notes 纯文本；
- 用户自己的微信读书书目、划线和个人笔记；
- 写回校验所需的笔记元数据、正文哈希与修改状态。

连续原文定位在本机 Notes 自动化进程中完成，只返回匹配笔记的标题、文件夹、修改时间和 Note ID。多条命中时不会先读取候选正文或生成回答。

## 数据会发送到哪里

- `WEREAD_API_KEY` 只作为 Bearer 凭证发送到 `https://i.weread.qq.com/api/agent/gateway`。脚本拒绝把该凭证发送到其他网关。
- 为完成用户请求，目标备忘录的相关正文、检索到的划线和个人笔记会进入当前 Codex 会话的模型上下文。
- Reading Mirror 不包含自己的分析服务、遥测、数据库或后台进程。

请根据你使用的 Codex 产品、账户和组织政策确认模型数据处理方式。不要对不希望进入当前 Codex 会话的备忘录运行本 Skill。

## 本地保存什么

默认目录：

```text
~/Library/Application Support/Codex/reading-mirror/
```

其中：

- `mirror/` 保存微信读书划线和个人笔记的本地 Markdown 镜像、书目元数据、同步时间与内容哈希；
- `state/` 保存 Apple Note ID、标题、长度、哈希与运行时间，用于判断新增内容和并发冲突；
- `state/` 不保存完整 Apple 备忘录正文，但标题和 Note ID 本身仍属于个人数据。

这些目录和文件会使用仅当前用户可访问的权限创建。也可以通过 `READING_MIRROR_HOME` 改变存储位置。

## 凭证

`WEREAD_API_KEY` 只从当前进程环境读取，不写入 Skill、镜像或状态文件。不要把密钥放入仓库、命令历史、截图或公开日志。

## Apple Notes 写入保护

- 密码保护笔记不会写入；
- 含附件的笔记不会通过 HTML 桥写入；
- 共享笔记需要操作时确认；
- 写入前后会检查正文哈希和完整逻辑文本；
- 原生引用样式不可用时，不会把仅有 HTML 标签误报为完成。

## 删除本地数据

退出 Apple Notes 和相关 Codex 任务后，用户可以删除自己的 Reading Mirror 运行数据目录。删除镜像不会删除 Apple Notes 或微信读书中的原始内容，但会清除本地同步数据和增量处理状态。

请勿删除或分享他人的数据，也不要把运行数据目录和本仓库一起发布。
